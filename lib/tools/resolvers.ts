/**
 * @author ColdByDefault
 * @copyright 2026 ColdByDefault. All Rights Reserved.
 */

import "server-only";

// Shared name→ID resolution helpers.
// Add resolvers here as features grow (e.g. resolveContactIdsByNames).
// Tasks don't require name resolution — this file is a placeholder.

export {};
